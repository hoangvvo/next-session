{
  "name": "next-session",
  "version": "2.0.2",
  "description": "Simple promise-based session middleware for Next.js",
  "keywords": [
    "javascript",
    "nextjs",
    "session",
    "middleware",
    "promise"
  ],
  "main": "src/index.js",
  "files": [
    "src/"
  ],
  "scripts": {
    "lint": "eslint src -c .eslintrc --ext js --ignore-path .gitignore",
    "lint:fix": "eslint src -c .eslintrc --ext js --ignore-path .gitignore --fix",
    "test": "jest --coverage --coverageReporters=text-lcov > coverage.lcov",
    "test:local": "jest --coverage"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hoangvvo/next-session.git"
  },
  "author": "Hoang Vo (https://www.hoangvvo.com)",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/hoangvvo/next-session/issues"
  },
  "homepage": "https://github.com/hoangvvo/next-session#readme",
  "devDependencies": {
    "eslint": "^6.1.0",
    "eslint-config-airbnb-base": "^14.0.0",
    "eslint-plugin-import": "^2.18.2",
    "jest": "^24.8.0",
    "supertest": "^4.0.2"
  },
  "dependencies": {
    "cookie": "^0.4.0"
  },
  "engines": {
    "node": ">=8.0.0"
  }
}
